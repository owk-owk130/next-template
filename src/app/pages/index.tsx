import DefaultLayout from "~/layout/defaultLayout";
import { HomePage } from "~/features/Home";

const Home = () => {
  return (
    <DefaultLayout>
      <HomePage />
    </DefaultLayout>
  );
};

export default Home;
